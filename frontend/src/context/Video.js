export default class Video {
    constructor({
        id, name, url, recommendations
    })
    {
        this.id = id;
        this.name = name;
        this.url = url;
        this.recommendations = recommendations;
    }
}
